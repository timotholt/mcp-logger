{
  "name": "@mcp-logger/shared",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist && babel src --out-dir dist --extensions .js",
    "test": "vitest",
    "lint": "eslint src"
  },
  "dependencies": {},
  "devDependencies": {
    "rimraf": "^5.0.10"
  }
}
